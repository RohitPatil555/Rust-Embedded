
To compile this code use below command
```
rust default nightly
```

We are using nightly channel as alloc trait have unstable feture alloc_error_handler.
Once it release as stable, it can be compile or update for stable release.

